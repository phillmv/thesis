%html
  %head 
    %title
      thesis
  = stylesheet_link_tag 'application', '960.css'
  = javascript_include_tag "jquery-1.4.2.min", "jquery.infinitescroll", "application"
#header.container_12
  .grid_2
    %br/
  .grid_8
    %h1
      Hello World.

    - if !@current_user.nil? then
      -# form_for :subscription, :url => subscriptions_path do |f|
        = text_field :subscription, :feed_url
        = f.submit 'add sub'
      = "user: #{@current_user.id}"
      = link_to "edit subscriptions", subscriptions_path
      •      
      = link_to "edit classifications", "#"
      •
      = link_to "logout", '/logout'
      
  -#.grid_4
    = will_paginate @entries, :id => true

#burden.container_12
  = yield
